import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Toaster } from "react-hot-toast";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Dice Verse - Play Adventures Online",
  description:
    "Create and play immersive RPG campaigns with friends online. Turn-based gameplay, interactive scenarios, and collaborative storytelling.",
  keywords: "RPG, tabletop, online gaming, multiplayer, adventures, campaigns",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
        <Toaster position="top-right" />
      </body>
    </html>
  );
}
